export interface ICard {
    id: string,
    badges: any,
    name: string,
    due: string,
    dueComplete: boolean,
    idList: string,
    idLabels: string[],
    pos: number,
    shortUrl: string
}
